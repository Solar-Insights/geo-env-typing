{
    "name": "solar-typing",
    "version": "1.3.1",
    "description": "A package for typing and value-checking of various environmental data. Based mainly on Google's APIs. ",
    "type": "module",
    "scripts": {
        "deploy": "npm run clean && npm run build && npm publish",
        "clean": "del /S /Q .\\dist\\*.js .\\dist\\*.d.ts > NUL",
        "build": "npm run prettier && npx tsc --project . --declaration",
        "prettier": "npx prettier --trailing-comma none --tab-width 4 --print-width 120 -uw --cache --ignore-path .gitignore ."
    },
    "author": "MathBeaudoin",
    "types": "./dist/index.d.ts",
    "devDependencies": {
        "prettier": "^3.2.5",
        "typescript": "^5.3.3"
    }
}
